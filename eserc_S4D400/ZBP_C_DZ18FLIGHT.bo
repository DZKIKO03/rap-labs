projection implementation in class ZBP_C_DZ18FLIGHT unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_DZ18FLIGHT alias ZcDz18flight
extensible
use etag
{
  field ( readonly )
  PlaneTypeID;

//  use create;
    use update;
  //  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}
